# Upstream ownership and local modifications notice

This repository is a modified fork of the OpenSSL Project source tree. The OpenSSL work, its architecture, implementation, documentation, and upstream authorship remain attributable to the OpenSSL Project Authors and other copyright holders identified by the upstream source and `AUTHORS.md`. The repository retains the upstream `LICENSE.txt` (Apache License 2.0 for the current OpenSSL line).

Upstream project: https://github.com/openssl/openssl

Local commits, maintenance, ports, build changes, new files, or other modifications in this fork do **not** by themselves establish independent authorship of the OpenSSL work or of any structure, logic, architecture, decomposition, interfaces, or implementation derived from it. A change is treated as an independently authored component only when its independent conception, structure, logic, implementation, and provenance can be demonstrated separately from upstream.

The `rmr/` additions and other fork-only changes are therefore not globally classified here as independently authored merely because they were added after the upstream fork point or use distinct code. Their per-component independence classification remains `TOKEN_VAZIO_PER_COMPONENT` until structural/logical provenance review is complete.

Technical facts, standards, protocol requirements, APIs, and interoperability constraints may be referenced without implying ownership of those facts or standards. Where the legal consequence of a particular similarity, interface, algorithm, contribution, or modification is uncertain, the state is `LEGAL_REVIEW_REQUIRED` rather than a definitive legal conclusion.

This notice is attribution/provenance metadata only. It does not replace or modify `LICENSE.txt`, upstream copyright notices, third-party notices, or file-specific licensing terms.
